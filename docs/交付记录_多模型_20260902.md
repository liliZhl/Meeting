# 多模型版本 EXE 交付记录（2026-09-02 17:30）

## 交付物
`C:\Users\<用户名>\Desktop\XPI\Meeting\dist\MeetingAssistant\`（总 9.03 GB）
- **MeetingAssistant.exe** 88.6MB（17:24:29 打包，含多模型 + 幻觉抑制 + 模型切换）
- `_internal/` 4.0GB（13475 文件）
- `mod/` 4.8GB — **6 个模型**：fun-asr-nano(2046MB) / sensevoice(896MB) / paraformer(848MB) / cam++(27.5MB) / ct-punc(1131.8MB) / fsmn-vad(3.8MB)
- `bin/` 98.1MB（ffmpeg.exe）
- `logs/`

## 本次新增功能
1. **多模型可选**：设置里下拉框选 语音识别模型（fun-asr-nano / sensevoice / paraformer）
   - fun-asr-nano：LLM 高精度，GPU 旗舰（默认）
   - sensevoice：CPU 快（RTF 0.085），50+ 语种
   - paraformer：CPU 最快（RTF 0.08），中文准 + 字级时间戳 + 带标点切句
2. **模型切换**：配置弹窗改模型后自动重置常驻模型并重新预加载，下次转写即用新模型
3. **幻觉抑制**（上轮已合）：Fun-ASR-Nano llm_kwargs repetition_penalty=1.15 + no_repeat_ngram_size=3

## 实测数据（开发机 CPU，285s 音频）
| 模型 | 转写耗时 | 句子数 | 特点 |
|---|---|---|---|
| fun-asr-nano | ~135s | 76 | 质量高（GPU 下大幅加速） |
| sensevoice | 24.9s | 105(VAD粒度) | CPU 最快档之一 |
| paraformer | 22.8s | 94(带标点) | CPU 快 + 细粒度 |

## 打包验证
- 启动 EXE → 首次运行弹配置引导 → 预加载 fun-asr-nano → 状态 ready（CPU ~2.5min）
- 日志确认：`ASREngine 初始化 ... asr_model=fun-asr-nano`（从 config 读取）
- `模型状态检查: ✅ 模型已就绪（Fun-ASR-Nano）`

## git
- 2036bd5 feat: 多模型可选 + 标签清理 + 切换重置
- 64a2ccd docs: 工作日志
